import React from 'react'
import module from '../../css/hash.module.scss'

interface Props { }

function Hash(props: Props) {
  const { } = props

  return (
    <>
      <section className={module.hash}>
        hash
      </section>
    </>
  )
}

export default Hash
